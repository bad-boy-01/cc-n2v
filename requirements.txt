# CC-Novel2Video — Free Kaggle-Compatible Stack
# Zero paid APIs. Runs on Kaggle 2x T4 GPUs.

# ── Web Framework ────────────────────────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
pydantic>=2.0.0

# ── Core Utilities ───────────────────────────────────────
Pillow>=10.0.0
numpy>=1.23.0
PyYAML>=6.0.1
requests>=2.28.0
json-repair>=0.25.0

# ── LLM: Qwen3 / Qwen2.5 / DeepSeek-R1 (local) ─────────
torch>=2.1.0
transformers>=4.45.0
accelerate>=0.30.0
bitsandbytes>=0.43.0          # 4-bit quantization for T4
huggingface_hub>=0.23.0
sentencepiece>=0.1.99          # Qwen tokenizer

# ── Image Generation: FLUX.1-schnell + SDXL ─────────────
diffusers>=0.30.0
xformers>=0.0.27               # memory-efficient attention
compel>=2.0.0                  # prompt weighting for SDXL

# ── Audio: Kokoro TTS + edge-tts fallback ───────────────
kokoro>=0.9.2
soundfile>=0.12.1
edge-tts>=6.1.9

# ── OCR: EasyOCR (manhwa mode) ──────────────────────────
easyocr>=1.7.0

# ── Video Assembly ───────────────────────────────────────
moviepy>=2.0.0
ffmpeg-python>=0.2.0
opencv-python>=4.9.0

# ── Database / Queue (existing) ──────────────────────────
# SQLite is stdlib — no extra package needed

# ── Optional: Piper TTS fallback ────────────────────────
# piper-tts         # install separately on Kaggle if needed
